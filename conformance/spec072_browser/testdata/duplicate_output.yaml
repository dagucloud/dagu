llm:
  provider: local
  model: test-model
steps:
  - id: checkout
    action: browser.run
    with:
      url: https://shop.example.com
      do:
        - extract:
            instruction: The order number
            schema:
              type: object
              properties:
                order: {type: string}
        - extract:
            instruction: The order again
            schema:
              type: object
              properties:
                order: {type: string}
