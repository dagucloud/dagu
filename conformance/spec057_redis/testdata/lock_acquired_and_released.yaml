env:
  - REDIS_URL: ${REDIS_URL}
  - REDIS_PREFIX: ${REDIS_PREFIX}
steps:
  - action: redis.set
    with:
      url: $REDIS_URL
      key: "${REDIS_PREFIX}locktest"
      value: v1
      lock: "${REDIS_PREFIX}mylock"
