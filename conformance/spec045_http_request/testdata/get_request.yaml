env:
  - HTTP_URL: ${HTTP_URL}
steps:
  - action: http.request
    with:
      method: GET
      url: $HTTP_URL/get
      headers:
        X-Test-Header: header-value
      query:
        q: query-value
