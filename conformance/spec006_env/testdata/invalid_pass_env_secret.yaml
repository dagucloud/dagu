secrets:
  - name: API_TOKEN
    provider: env
    key: SOURCE_PASS_SECRET
steps:
  - name: call-child
    action: dag.run
    with:
      dag: pass_env_secret_child
    pass_env: [API_TOKEN]

---
name: pass_env_secret_child
steps:
  - name: noop
    run: echo child
