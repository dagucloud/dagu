env:
  - SSH_PORT: ${SSH_PORT}
steps:
  - name: upload
    action: sftp.upload
    with:
      host: 127.0.0.1
      port: $SSH_PORT
      user: sshtest
      password: sshtestpass
      strict_host_key: false
      source: upload_source.txt
      destination: /home/sshtest/uploaded_single.txt
  - name: readback
    depends: upload
    action: ssh.run
    with:
      host: 127.0.0.1
      port: $SSH_PORT
      user: sshtest
      password: sshtestpass
      strict_host_key: false
      command: "stat -c %a /home/sshtest/uploaded_single.txt && cat /home/sshtest/uploaded_single.txt"
