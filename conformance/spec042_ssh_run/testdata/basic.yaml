env:
  - SSH_PORT: ${SSH_PORT}
  - GREETING: hello
steps:
  - action: ssh.run
    with:
      host: 127.0.0.1
      port: $SSH_PORT
      user: sshtest
      password: sshtestpass
      strict_host_key: false
      command: echo "$GREETING from remote"
    output: OUT
