env:
  - SSH_PORT: ${SSH_PORT}
steps:
  - action: ssh.run
    timeout_sec: 1
    with:
      host: 127.0.0.1
      port: $SSH_PORT
      user: sshtest
      password: sshtestpass
      strict_host_key: false
      command: sleep 3 && touch /tmp/dagu-ssh-timeout-marker
