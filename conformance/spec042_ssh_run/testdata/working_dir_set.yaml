env:
  - SSH_PORT: ${SSH_PORT}
steps:
  - action: ssh.run
    working_dir: /tmp
    with:
      host: 127.0.0.1
      port: $SSH_PORT
      user: sshtest
      password: sshtestpass
      strict_host_key: false
      command: pwd
