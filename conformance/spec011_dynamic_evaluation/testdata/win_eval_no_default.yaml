shell: cmd
working_dir: .
params:
  - name: run_date
    type: string
    eval: "`CommandThatDoesNotExistSpec011`"
steps:
  - name: write
    run: <nul set /p dummy=unexpected > windows-should-not-run.txt
