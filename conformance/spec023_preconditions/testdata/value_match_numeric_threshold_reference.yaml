working_dir: .
params:
  - threshold: "0.8"
steps:
  - id: write
    preconditions:
      - condition: "0.9"
        expected: "num:>=${threshold}"
    run: printf '%s\n' ran > numeric-threshold-ran.txt
