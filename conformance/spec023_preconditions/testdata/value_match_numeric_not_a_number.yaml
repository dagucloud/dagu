working_dir: .
steps:
  - id: should_fail
    preconditions:
      - condition: "$DAGU_CONFORMANCE_UNDEFINED_NUMBER"
        expected: "num:>=0.8"
    run: printf '%s\n' bad > numeric-not-a-number-ran.txt
