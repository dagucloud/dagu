params:
  - name: environment
    description: Runtime environment.
working_dir: .
steps:
  - name: should-not-run
    run: |
      printf 'ran\n' > should-not-exist-run.txt
      printf '%s\n' '${params.environment}'
