params:
  - name: environment
    description: Runtime environment.
working_dir: .
steps:
  - name: should-not-write
    action: file.write
    with:
      path: should-not-exist-with-action.txt
      content: "${params.environment}"
