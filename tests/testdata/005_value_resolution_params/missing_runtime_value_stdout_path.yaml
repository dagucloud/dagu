params:
  - name: environment
    description: Runtime environment.
working_dir: .
steps:
  - name: should-not-run
    run: |
      printf 'ran\n' > should-not-exist-stdout.txt
      printf 'hello\n'
    stdout: stdout-${params.environment}.txt
